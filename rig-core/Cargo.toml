[package]
name = "rig-core"
version = "0.1.0"
edition = "2021"

[lib]
path="src/lib.rs"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
reqwest = { version = "0.11.22", features = ["json"] }
serde = { version = "1.0.193", features = ["derive"] }
serde_json = "1.0.108"
tokio = { version = "1.34.0", features = ["full"] }
tracing = "0.1.40"
futures = "0.3.29"
anyhow = "1.0.75"
ordered-float = "4.2.0"
schemars = "0.8.16"
mongodb = "2.8.2"

[dev-dependencies]
# aws-config = "1.1.9"
# mongodb = { version = "2.8.2", features = ["aws-auth"] }
# mongodb-utils = { path = "../../shared/rust/mongodb-utils" }
# subgraph-client = { path = "../../shared/rust/subgraph-client", version = "0.1.0" }
tracing-subscriber = "0.3.18"
