when:
  path: /chat/completions
  method: POST
  query_param: []
  header:
  - name: accept
    value: text/event-stream
  - name: content-type
    value: application/json
  body: '{"max_tokens":8,"messages":[{"content":"Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Answer with exactly one word: cached.","role":"user"}],"model":"deepseek-v4-flash","stream":true,"stream_options":{"include_usage":true},"thinking":{"type":"disabled"}}'
then:
  status: 200
  header:
  - name: content-type
    value: text/event-stream; charset=utf-8
  body: |+
    data: {"choices":[{"delta":{"content":"","role":"assistant"},"finish_reason":null,"index":0,"logprobs":null}],"created":0,"id":"ed05818a-38b1-435f-914b-410ecb92ffb4","model":"deepseek-flash","object":"chat.completion.chunk","system_fingerprint":"aeb56401ca74e127821c4f9126dcb669","usage":null}

    data: {"choices":[{"delta":{"content":"c"},"finish_reason":null,"index":0,"logprobs":null}],"created":0,"id":"ed05818a-38b1-435f-914b-410ecb92ffb4","model":"deepseek-flash","object":"chat.completion.chunk","system_fingerprint":"aeb56401ca74e127821c4f9126dcb669","usage":null}

    data: {"choices":[{"delta":{"content":"ached"},"finish_reason":null,"index":0,"logprobs":null}],"created":0,"id":"ed05818a-38b1-435f-914b-410ecb92ffb4","model":"deepseek-flash","object":"chat.completion.chunk","system_fingerprint":"aeb56401ca74e127821c4f9126dcb669","usage":null}

    data: {"choices":[{"delta":{"content":""},"finish_reason":"stop","index":0,"logprobs":null}],"created":0,"id":"ed05818a-38b1-435f-914b-410ecb92ffb4","model":"deepseek-flash","object":"chat.completion.chunk","system_fingerprint":"aeb56401ca74e127821c4f9126dcb669","usage":{"completion_tokens":2,"prompt_cache_hit_tokens":766,"prompt_cache_miss_tokens":134,"prompt_tokens":900,"prompt_tokens_details":{"cached_tokens":766},"total_tokens":902}}

    data: [DONE]

---
when:
  path: /chat/completions
  method: POST
  query_param: []
  header:
  - name: accept
    value: text/event-stream
  - name: content-type
    value: application/json
  body: '{"max_tokens":8,"messages":[{"content":"Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Answer with exactly one word: cached.","role":"user"}],"model":"deepseek-v4-flash","stream":true,"stream_options":{"include_usage":true},"thinking":{"type":"disabled"}}'
then:
  status: 200
  header:
  - name: content-type
    value: text/event-stream; charset=utf-8
  body: |+
    data: {"choices":[{"delta":{"content":"","role":"assistant"},"finish_reason":null,"index":0,"logprobs":null}],"created":0,"id":"9ab11c2a-09cc-495f-b1d6-166c378b35bf","model":"deepseek-flash","object":"chat.completion.chunk","system_fingerprint":"aeb56401ca74e127821c4f9126dcb669","usage":null}

    data: {"choices":[{"delta":{"content":"c"},"finish_reason":null,"index":0,"logprobs":null}],"created":0,"id":"9ab11c2a-09cc-495f-b1d6-166c378b35bf","model":"deepseek-flash","object":"chat.completion.chunk","system_fingerprint":"aeb56401ca74e127821c4f9126dcb669","usage":null}

    data: {"choices":[{"delta":{"content":"ached"},"finish_reason":null,"index":0,"logprobs":null}],"created":0,"id":"9ab11c2a-09cc-495f-b1d6-166c378b35bf","model":"deepseek-flash","object":"chat.completion.chunk","system_fingerprint":"aeb56401ca74e127821c4f9126dcb669","usage":null}

    data: {"choices":[{"delta":{"content":""},"finish_reason":"stop","index":0,"logprobs":null}],"created":0,"id":"9ab11c2a-09cc-495f-b1d6-166c378b35bf","model":"deepseek-flash","object":"chat.completion.chunk","system_fingerprint":"aeb56401ca74e127821c4f9126dcb669","usage":{"completion_tokens":2,"prompt_cache_hit_tokens":766,"prompt_cache_miss_tokens":134,"prompt_tokens":900,"prompt_tokens_details":{"cached_tokens":766},"total_tokens":902}}

    data: [DONE]

