# rig-runtime-conformance

Unpublished, test-only fixtures for checking the observable behavior of Rig's
independent classic and ECS runtimes. This crate deliberately contains no
runtime trait and no production orchestration implementation.
