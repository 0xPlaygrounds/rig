# Allowlist for the serde policy wall (see driver_adoption.rs,
# `provider_streaming_modules_never_raw_parse_the_wire`).
#
# Format: `path suffix | line snippet | one-line justification`.
# Every entry must justify why the raw parse is NOT wire-frame triage:
# frame decoding belongs to the wire.rs classifiers and the adapter driver.
# Stale entries (matching no shipped line) fail the guard — delete them
# alongside the code they covered.

# --- shared Chat Completions-compatible helper: in-band error triage -------
providers/internal/openai_chat_completions_compatible.rs | serde_json::from_str::<serde_json::Value>(data).ok()?; | provider_response_from_compatible_sse_data deliberately runs pre-classification: compatible gateways send in-band error envelopes as data frames ("Treat the chunk as an error only when `error` is present AND carries a payload ... canonical OpenAI-compatible error event"), so it peeks for a populated `error` key (with no `choices`) to surface a CompletionError; unparseable or non-error chunks fall through to the classifier untouched.

# --- shared Chat Completions-compatible helper: blocking choice decode -----
providers/internal/openai_chat_completions_compatible.rs | && serde_json::from_value::<T>(repaired).is_ok() | Validates a repaired copy of an already-deserialized blocking response choice before dropping incomplete tool arguments; it cannot classify or skip a streaming frame, and failure keeps the original choice on the loud decode path.
providers/internal/openai_chat_completions_compatible.rs | serde_json::from_value(choice).map_err(serde::de::Error::custom) | Performs the final typed decode of an already-deserialized blocking response choice after output-length tool-call policy; every failure is returned as a serde error, never triaged or skipped as a stream frame.

# --- anthropic: content assembly, not frame triage -------------------------
providers/anthropic/streaming.rs | citation: serde_json::from_value(citation) | Field decode inside the content-delta Deserialize impl: converts the `citation` value of an already-tag-matched `citations_delta` into its typed form; failure is a serde::de error and unrecognized tags fall to Self::Unknown, so no frame is triaged here.
providers/anthropic/streaming.rs | serde_json::from_str(&server_tool_use.input_json) | Assembles the accumulated server_tool_use partial_json fragments into the raw-content value; malformed input surfaces as a CompletionError, no frame is triaged here.

# --- openai Responses SSE: classifier-adjacent helpers ---------------------
providers/openai/responses_api/streaming.rs | let value = serde_json::from_str::<serde_json::Value>(data).ok()?; | provider_response_from_responses_sse_data peeks at the `type` tag to build the provider-error body; classification already happened.
providers/openai/responses_api/streaming.rs | serde_json::from_str::<StreamingCompletionChunk>(data) | parse_sse_completion_body: unary (non-stream) replay of an SSE body into a CompletionResponse; terminal extraction, not per-frame triage.
providers/openai/responses_api/streaming.rs | let Ok(value) = serde_json::from_str::<serde_json::Value>(data) else | parse_sse_completion_body: fallback terminal extraction for envelope-less unary bodies; unmatched frames are not policy-decided here.
providers/openai/responses_api/streaming.rs | completed = Some(serde_json::from_value(response.clone())?); | parse_sse_completion_body: decodes the terminal `response` payload of a completed/incomplete unary body; failure is an error.
providers/openai/responses_api/streaming.rs | let mut value = serde_json::from_str::<serde_json::Value>(data).ok()?; | repair_envelope_less_frame: buffered-body envelope repair feeding the ONE classifier (classify_with_repair); documented F8 policy note at the fn doc.
providers/openai/responses_api/streaming.rs | let kind = serde_json::from_str::<serde_json::Value>(&data) | Reads the `type` tag only to word the buffered-path Corrupt error message; the classification itself came from classify_with_repair.
# Single-file providers: unary/non-stream parses sharing a file with streaming code.
providers/ollama.rs | serde_json::from_slice(&bytes)?; | unary embedding response body, not a stream frame
providers/copilot/mod.rs | CopilotEmbeddingResponse = match serde_json::from_slice | unary embedding response body, not a stream frame
providers/copilot/mod.rs | serde_json::from_slice::<NestedApiError>(&body) | unary error-envelope decode on a non-2xx body, not a stream frame

# --- #[serde(untagged)] unions that are not frame triage --------------------
providers/ollama.rs | #[serde(untagged)] | The `Think` request-option value (`true`/`false` or a named level) is an untagged union on the REQUEST body; nothing about inbound frame policy is decided here.
providers/openai/responses_api/streaming.rs | #[serde(untagged)] | Two sites, neither a triage fallback: `StreamingCompletionChunk` is the envelope union (response chunk vs item delta) decoded AFTER `classify_responses_frame` recognized the frame; `ContentPartChunkPart::Unknown` is untagged on the SERIALIZE side only — its Deserialize is the hand-written tag dispatch documented beside it, so a known tag with a defective payload still errors.
providers/openai/completion/streaming.rs | #[serde(untagged)] | `FinishReason::Other(String)` preserves an unrecognized finish-reason token verbatim (surfacing gateway-invented reasons instead of misreading them as a stop); a scalar vocabulary field, not frame policy.
providers/copilot/mod.rs | #[serde(untagged)] | `ChatApiResponse<T>` distinguishes a success body from Copilot's error envelope on a UNARY response (no stream frame involved).

# --- openai responses websocket: envelope dispatch, not frame triage -------
# The websocket carries the SAME Responses events the SSE path carries, and
# `parse_server_event` routes every one of them through `classify_responses_frame`.
# What it decodes itself is only the two envelopes the websocket adds around
# that shared set: the transport-level `error` event and the terminal
# `response.done`. Neither exists on the SSE wire, so neither has a classifier
# to route to.
providers/openai/responses_api/websocket.rs | serde_json::from_str::<EventType>(payload) | Reads only the `type` tag to decide whether the frame is one of the two websocket-only envelopes; every other frame is handed to classify_responses_frame untouched, so this dispatches rather than triages.
providers/openai/responses_api/websocket.rs | "error" => serde_json::from_str(payload) | Decodes the websocket-only `error` envelope, which has no SSE counterpart and therefore no classifier; a decode failure is returned as a CompletionError, never skipped.
providers/openai/responses_api/websocket.rs | "response.done" => serde_json::from_str(payload) | Decodes the websocket-only `response.done` envelope, which has no SSE counterpart and therefore no classifier; a decode failure is returned as a CompletionError, never skipped.
providers/openai/responses_api/websocket.rs | serde_json::from_value(status) | Reads the `status` field of an already-decoded `response.done` payload to update the session's previous_response_id bookkeeping; it decides no frame's fate.
providers/openai/responses_api/websocket.rs | serde_json::from_value(self.response.clone()) | Converts an already-decoded `response.done` payload into the typed terminal response; `None` falls back to the session's explicit no-terminal-body error rather than skipping anything.
