# Examples

## Standalone examples

examples are standalone binary crates in the `examples` folder. To run `examples/agent` (for example): `cargo run -p example-agent`.

## Legacy examples

Legacy examples live under `crates/rig/examples/`. Run them with:

```bash
cargo run --example <name>
```

These are being gradually migrated to the standalone pattern.
