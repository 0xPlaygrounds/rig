{
  "scenarios": [
    {
      "assertion_mappings": [],
      "assertion_sources": [
        {
          "path": "tests/providers/together/agent.rs",
          "symbol": "together::agent::completion_smoke"
        },
        {
          "path": "tests/common/support.rs",
          "symbol": "support::assert_nonempty_response"
        }
      ],
      "baseline_ignored": true,
      "classification": "agent",
      "classification_reason": "OC-A7 live agent completion smoke (tests/ecs_parity/openai-compatible-classification-contract.md): `together::Client::from_env().agent(MIXTRAL_8X7B_INSTRUCT_V0_1).preamble(BASIC_PREAMBLE).build().prompt(BASIC_PROMPT)`; `assert_nonempty_response`. `#[ignore]` live; no credential configured.",
      "configuration": "root-bedrock",
      "declared_gates": [
        "# [ignore = \"requires TOGETHER_API_KEY\"]"
      ],
      "ecs": null,
      "evidence_scope": "supplemental_live",
      "families": [
        "provider-completions"
      ],
      "family_contract": "tests/ecs_parity/openai-compatible-classification-contract.md",
      "fixture_disposition": "live only; no recording at the base",
      "fixtures": [],
      "id": "rig::together::together::agent::completion_smoke",
      "mapping_limits": [
        "Ignored live row: discovered, never executed; counted as supplemental_live, not ported. A phase-2 isolated capture is planned only where a credential is configured (groq, perplexity)."
      ],
      "source": "tests/providers/together/agent.rs"
    },
    {
      "assertion_mappings": [],
      "assertion_sources": [
        {
          "path": "tests/providers/together/context.rs",
          "symbol": "together::context::context_smoke"
        },
        {
          "path": "tests/common/support.rs",
          "symbol": "support"
        }
      ],
      "baseline_ignored": true,
      "classification": "agent",
      "classification_reason": "OC-A1 via OC-A7 agent blocking run (tests/ecs_parity/openai-compatible-classification-contract.md): `together::Client::from_env()`; `CONTEXT_DOCS.iter().fold(client.agent(MIXTRAL_8X7B_INSTRUCT_V0_1), AgentBuilder::context).build().prompt(CONTEXT_PROMPT)`; `assert_contains_any_case_insensitive`. `#[ignore]` live, no cassette, no credential configured.",
      "configuration": "root-bedrock",
      "declared_gates": [
        "# [ignore = \"requires TOGETHER_API_KEY\"]"
      ],
      "ecs": null,
      "evidence_scope": "supplemental_live",
      "families": [
        "context"
      ],
      "family_contract": "tests/ecs_parity/openai-compatible-classification-contract.md",
      "fixture_disposition": "live only; no recording at the base",
      "fixtures": [],
      "id": "rig::together::together::context::context_smoke",
      "mapping_limits": [
        "Ignored live row: discovered, never executed at the base; no cassette.",
        "Classified only; no native counterpart yet (ecs null). Obligations to map in phase 2 are the ones named in the reason."
      ],
      "source": "tests/providers/together/context.rs"
    },
    {
      "assertion_mappings": [],
      "assertion_sources": [
        {
          "path": "tests/providers/together/embeddings.rs",
          "symbol": "together::embeddings::embeddings_smoke"
        },
        {
          "path": "tests/common/support.rs",
          "symbol": "support"
        }
      ],
      "baseline_ignored": true,
      "classification": "shared_provider",
      "classification_reason": "OC-C1 embeddings (tests/ecs_parity/openai-compatible-classification-contract.md): `together::Client::from_env().embedding_model(together::embedding::M2_BERT_80M_8K_RETRIEVAL).embed_texts(EMBEDDING_INPUTS)`; `assert_embeddings_nonempty_and_consistent`. Embeddings path; `#[ignore]` live.",
      "configuration": "root-bedrock",
      "declared_gates": [
        "# [ignore = \"requires TOGETHER_API_KEY\"]"
      ],
      "ecs": null,
      "evidence_scope": "shared_provider",
      "families": [
        "embeddings"
      ],
      "family_contract": "tests/ecs_parity/openai-compatible-classification-contract.md",
      "fixture_disposition": "live only; no recording at the base",
      "fixtures": [],
      "id": "rig::together::together::embeddings::embeddings_smoke",
      "mapping_limits": [
        "Ignored live row: discovered, never executed at the base; no cassette.",
        "Shared-provider row: executes unchanged against rig-core provider code in both runtimes; never counted as an agent cell."
      ],
      "source": "tests/providers/together/embeddings.rs"
    },
    {
      "assertion_mappings": [],
      "assertion_sources": [
        {
          "path": "tests/providers/together/streaming.rs",
          "symbol": "together::streaming::streaming_smoke"
        },
        {
          "path": "tests/common/support.rs",
          "symbol": "support"
        }
      ],
      "baseline_ignored": true,
      "classification": "agent",
      "classification_reason": "OC-A2 via OC-A7 agent streaming run (tests/ecs_parity/openai-compatible-classification-contract.md): `client.agent(LLAMA_3_8B_CHAT_HF).preamble(STREAMING_PREAMBLE).build().stream_prompt(STREAMING_PROMPT).stream()` drained by `collect_stream_final_response`. `#[ignore]` live, no cassette, no credential configured.",
      "configuration": "root-bedrock",
      "declared_gates": [
        "# [ignore = \"requires TOGETHER_API_KEY\"]"
      ],
      "ecs": null,
      "evidence_scope": "supplemental_live",
      "families": [
        "streaming-smoke"
      ],
      "family_contract": "tests/ecs_parity/openai-compatible-classification-contract.md",
      "fixture_disposition": "live only; no recording at the base",
      "fixtures": [],
      "id": "rig::together::together::streaming::streaming_smoke",
      "mapping_limits": [
        "Ignored live row: discovered, never executed at the base; no cassette.",
        "Classified only; no native counterpart yet (ecs null). Obligations to map in phase 2 are the ones named in the reason."
      ],
      "source": "tests/providers/together/streaming.rs"
    },
    {
      "assertion_mappings": [],
      "assertion_sources": [
        {
          "path": "tests/providers/together/streaming_tools.rs",
          "symbol": "together::streaming_tools::streaming_tools_smoke"
        },
        {
          "path": "tests/common/support.rs",
          "symbol": "support"
        }
      ],
      "baseline_ignored": true,
      "classification": "agent",
      "classification_reason": "OC-A2 via OC-A7 agent streaming run (tests/ecs_parity/openai-compatible-classification-contract.md): `client.agent(LLAMA_2_70B_CHAT_TOGETHER).preamble(STREAMING_TOOLS_PREAMBLE).tool(Adder).tool(Subtract).build().stream_prompt(..).stream()` drained by `collect_stream_final_response`; asserts -3. `#[ignore]` live, no cassette, no credential configured.",
      "configuration": "root-bedrock",
      "declared_gates": [
        "# [ignore = \"requires TOGETHER_API_KEY\"]"
      ],
      "ecs": null,
      "evidence_scope": "supplemental_live",
      "families": [
        "streaming-tools"
      ],
      "family_contract": "tests/ecs_parity/openai-compatible-classification-contract.md",
      "fixture_disposition": "live only; no recording at the base",
      "fixtures": [],
      "id": "rig::together::together::streaming_tools::streaming_tools_smoke",
      "mapping_limits": [
        "Ignored live row: discovered, never executed at the base; no cassette.",
        "Classified only; no native counterpart yet (ecs null). Obligations to map in phase 2 are the ones named in the reason."
      ],
      "source": "tests/providers/together/streaming_tools.rs"
    },
    {
      "assertion_mappings": [],
      "assertion_sources": [
        {
          "path": "tests/providers/together/tools.rs",
          "symbol": "together::tools::tools_smoke"
        },
        {
          "path": "tests/common/support.rs",
          "symbol": "support"
        }
      ],
      "baseline_ignored": true,
      "classification": "agent",
      "classification_reason": "OC-A1 via OC-A7 agent blocking run (tests/ecs_parity/openai-compatible-classification-contract.md): `client.agent(MIXTRAL_8X7B_INSTRUCT_V0_1).preamble(TOOLS_PREAMBLE).tool(Adder).tool(Subtract).build().prompt(TOOLS_PROMPT)`; asserts -3. `#[ignore]` live, no cassette, no credential configured.",
      "configuration": "root-bedrock",
      "declared_gates": [
        "# [ignore = \"requires TOGETHER_API_KEY\"]"
      ],
      "ecs": null,
      "evidence_scope": "supplemental_live",
      "families": [
        "tools-smoke"
      ],
      "family_contract": "tests/ecs_parity/openai-compatible-classification-contract.md",
      "fixture_disposition": "live only; no recording at the base",
      "fixtures": [],
      "id": "rig::together::together::tools::tools_smoke",
      "mapping_limits": [
        "Ignored live row: discovered, never executed at the base; no cassette.",
        "Classified only; no native counterpart yet (ecs null). Obligations to map in phase 2 are the ones named in the reason."
      ],
      "source": "tests/providers/together/tools.rs"
    }
  ],
  "schema": 1
}
