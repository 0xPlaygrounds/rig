{
  "scenarios": [
    {
      "assertion_mappings": [],
      "assertion_sources": [
        {
          "path": "tests/providers/azure/structured_output.rs",
          "symbol": "azure::structured_output::test_azure_structured_output"
        }
      ],
      "baseline_ignored": true,
      "classification": "agent",
      "classification_reason": "OC-A1 via OC-A7 agent blocking run (tests/ecs_parity/openai-compatible-classification-contract.md): `azure::Client::from_env()?.agent(openai::GPT_5_MINI).preamble(..).max_tokens(100).output_schema::<Person>().build().prompt_typed(..)`; asserts name/age. `#[ignore]` live, no cassette, no credential configured.",
      "configuration": "root-bedrock",
      "declared_gates": [
        "# [ignore = \"requires AZURE_OPENAI_API_KEY and related Azure env vars\"]"
      ],
      "ecs": null,
      "evidence_scope": "supplemental_live",
      "families": [
        "structured-output"
      ],
      "family_contract": "tests/ecs_parity/openai-compatible-classification-contract.md",
      "fixture_disposition": "live only; no recording at the base",
      "fixtures": [],
      "id": "rig::azure::azure::structured_output::test_azure_structured_output",
      "mapping_limits": [
        "Ignored live row: discovered, never executed at the base; no cassette.",
        "Classified only; no native counterpart yet (ecs null). Obligations to map in phase 2 are the ones named in the reason."
      ],
      "source": "tests/providers/azure/structured_output.rs"
    },
    {
      "assertion_mappings": [],
      "assertion_sources": [
        {
          "path": "tests/providers/azure/transcription.rs",
          "symbol": "azure::transcription::transcription_smoke"
        },
        {
          "path": "tests/common/support.rs",
          "symbol": "support"
        }
      ],
      "baseline_ignored": true,
      "classification": "shared_provider",
      "classification_reason": "OC-C3 transcription (tests/ecs_parity/openai-compatible-classification-contract.md): `azure::Client::from_env().transcription_model(\"whisper\").transcription_request().load_file(AUDIO_FIXTURE_PATH).send()`; asserts non-empty text. Transcription path; `#[ignore]` live.",
      "configuration": "root-bedrock",
      "declared_gates": [
        "# [ignore = \"requires AZURE_OPENAI_API_KEY and related Azure env vars\"]"
      ],
      "ecs": null,
      "evidence_scope": "shared_provider",
      "families": [
        "transcription"
      ],
      "family_contract": "tests/ecs_parity/openai-compatible-classification-contract.md",
      "fixture_disposition": "live only; no recording at the base",
      "fixtures": [],
      "id": "rig::azure::azure::transcription::transcription_smoke",
      "mapping_limits": [
        "Ignored live row: discovered, never executed at the base; no cassette.",
        "Shared-provider row: executes unchanged against rig-core provider code in both runtimes; never counted as an agent cell."
      ],
      "source": "tests/providers/azure/transcription.rs"
    }
  ],
  "schema": 1
}
