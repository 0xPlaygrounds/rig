{
  "scenarios": [
    {
      "assertion_mappings": [],
      "assertion_sources": [
        {
          "path": "tests/providers/voyageai/embeddings.rs",
          "symbol": "voyageai::embeddings::embeddings_smoke"
        },
        {
          "path": "tests/common/support.rs",
          "symbol": "support"
        }
      ],
      "baseline_ignored": true,
      "classification": "shared_provider",
      "classification_reason": "OC-C1 embeddings (tests/ecs_parity/openai-compatible-classification-contract.md): `voyageai::Client::from_env().embedding_model(VOYAGE_3_LARGE).embed_texts(EMBEDDING_INPUTS)`; `assert_embeddings_nonempty_and_consistent`. Embeddings path; `#[ignore]` live.",
      "configuration": "root-bedrock",
      "declared_gates": [
        "# [ignore = \"requires VOYAGE_API_KEY\"]"
      ],
      "ecs": null,
      "evidence_scope": "shared_provider",
      "families": [
        "embeddings"
      ],
      "family_contract": "tests/ecs_parity/openai-compatible-classification-contract.md",
      "fixture_disposition": "live only; no recording at the base",
      "fixtures": [],
      "id": "rig::voyageai::voyageai::embeddings::embeddings_smoke",
      "mapping_limits": [
        "Ignored live row: discovered, never executed at the base; no cassette.",
        "Shared-provider row: executes unchanged against rig-core provider code in both runtimes; never counted as an agent cell."
      ],
      "source": "tests/providers/voyageai/embeddings.rs"
    },
    {
      "assertion_mappings": [],
      "assertion_sources": [
        {
          "path": "tests/providers/voyageai/rerank.rs",
          "symbol": "voyageai::rerank::rerank_smoke"
        },
        {
          "path": "tests/common/support.rs",
          "symbol": "support"
        }
      ],
      "baseline_ignored": true,
      "classification": "shared_provider",
      "classification_reason": "OC-C6 rerank (tests/ecs_parity/openai-compatible-classification-contract.md): `voyageai::Client::from_env().rerank_model(RERANK_2_5).rerank(query, documents)`; asserts results, relevance, index, usage, model and provider. Rerank path; `#[ignore]` live.",
      "configuration": "root-bedrock",
      "declared_gates": [
        "# [ignore = \"requires VOYAGE_API_KEY\"]"
      ],
      "ecs": null,
      "evidence_scope": "shared_provider",
      "families": [
        "rerank"
      ],
      "family_contract": "tests/ecs_parity/openai-compatible-classification-contract.md",
      "fixture_disposition": "live only; no recording at the base",
      "fixtures": [],
      "id": "rig::voyageai::voyageai::rerank::rerank_smoke",
      "mapping_limits": [
        "Ignored live row: discovered, never executed at the base; no cassette.",
        "Shared-provider row: executes unchanged against rig-core provider code in both runtimes; never counted as an agent cell."
      ],
      "source": "tests/providers/voyageai/rerank.rs"
    }
  ],
  "schema": 1
}
