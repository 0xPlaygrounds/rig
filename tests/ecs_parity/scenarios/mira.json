{
  "scenarios": [
    {
      "assertion_mappings": [],
      "assertion_sources": [
        {
          "path": "tests/providers/mira/agent.rs",
          "symbol": "mira::agent::completion_smoke"
        },
        {
          "path": "tests/common/support.rs",
          "symbol": "support::assert_nonempty_response"
        }
      ],
      "baseline_ignored": true,
      "classification": "agent",
      "classification_reason": "Reviewed test constructs a rig-agent builder and executes prompt orchestration; includes golden/memory or live-model variants where present.",
      "configuration": "root-bedrock",
      "declared_gates": [
        "# [ignore = \"requires MIRA_API_KEY\"]"
      ],
      "ecs": null,
      "evidence_scope": "supplemental_live",
      "families": [
        "smoke"
      ],
      "family_contract": "tests/ecs_parity/mira-live-contract.md",
      "fixture_disposition": "MIRA_API_KEY is not configured in this environment; no capture possible; stays supplemental_live and is not counted as ported.",
      "fixtures": [],
      "id": "rig::mira::mira::agent::completion_smoke",
      "source": "tests/providers/mira/agent.rs"
    },
    {
      "assertion_mappings": [],
      "assertion_sources": [
        {
          "path": "tests/providers/mira/models.rs",
          "symbol": "mira::models::list_models_smoke"
        }
      ],
      "baseline_ignored": true,
      "classification": "shared_provider",
      "classification_reason": "Rule live-model in mira-live-contract.md: mira::Client::from_env().list_models().await.expect with a non-empty listing asserted; no agent; live, credential-gated.",
      "configuration": "root-bedrock",
      "declared_gates": [
        "# [ignore = \"requires MIRA_API_KEY\"]"
      ],
      "ecs": null,
      "families": [
        "provider"
      ],
      "family_contract": "tests/ecs_parity/mira-live-contract.md",
      "fixture_disposition": "Live-only: #[ignore] requires MIRA_API_KEY, not configured in this environment; no fixture on disk; stays supplemental_live, not counted as ported.",
      "fixtures": [],
      "id": "rig::mira::mira::models::list_models_smoke",
      "source": "tests/providers/mira/models.rs"
    },
    {
      "assertion_mappings": [],
      "assertion_sources": [
        {
          "path": "tests/providers/mira/tools.rs",
          "symbol": "mira::tools::tools_smoke"
        }
      ],
      "baseline_ignored": true,
      "classification": "agent",
      "classification_reason": "Rule live-agent in mira-live-contract.md: mira::Client::from_env().agent(anthropic::completion::CLAUDE_SONNET_4_6).preamble(TOOLS_PREAMBLE).tool(Adder).tool(Subtract).build().prompt(TOOLS_PROMPT).await.expect then assert_mentions_expected_number(-3); live, credential-gated.",
      "configuration": "root-bedrock",
      "declared_gates": [
        "# [ignore = \"requires MIRA_API_KEY\"]"
      ],
      "ecs": null,
      "evidence_scope": "supplemental_live",
      "families": [
        "tools"
      ],
      "family_contract": "tests/ecs_parity/mira-live-contract.md",
      "fixture_disposition": "Live-only: #[ignore] requires MIRA_API_KEY, not configured in this environment; no fixture on disk; stays supplemental_live, not counted as ported.",
      "fixtures": [],
      "id": "rig::mira::mira::tools::tools_smoke",
      "source": "tests/providers/mira/tools.rs"
    }
  ],
  "schema": 1
}
