# Supplemental gaps: documented rig-agent capabilities without an `agent` catalog row

Criterion: a capability documented in `crates/rig-agent/README.md`, the public `AgentBuilder`/`AgentRunner`/`Agent`/`AgentHook`/`Extractor`/`rig_agent::run` surface, `crates/rig-memory/README.md` or `crates/rig-ecs/CONTRACT.md` that no root `agent` row with `evidence_scope: cassette_agent` exercises at `a7eb63a91` (a `git grep` over the 88 original source files of the 350 root agent rows, plus `tests/common/{goldens,support}.rs` which those rows share). Companion rows (`tests/ecs_parity/inventory/companions.md`) are synthetic and are cited under "nearest fixture", never as coverage. Rows that exist but are `unclassified` in a provider file are named so the owning lane can classify them first.

One gap file per line: `superset-lanes/gaps/infrastructure-<slug>.md`, addressed to the lane in the last column.

| slug | capability | documented at | nearest existing fixture / needs synthetic | lane |
|---|---|---|---|---|
| `retrieval-dynamic-context` | `AgentBuilder::dynamic_context(samples, index)` / `dynamic_context_handler`: a `TopN` retrieval before every model call whose documents are patched into the request | crates/rig-agent/src/agent/builder.rs:208,230; crates/rig-verify/tests/corpus/mod.rs (retrieval axis); crates/rig-ecs/CONTRACT.md §12 | tests/providers/gemini/cassette/corpus_retrieval.rs (7 rows) and tests/providers/openai/cassette/corpus_retrieval.rs (5 rows), cassettes under tests/cassettes/{gemini,openai}/corpus_retrieval; goldens gemini_retrieval_*/openai_retrieval_* (rig-verify rows verify-corpus-engine/-world) | `gemini` |
| `retrieval-retrieved-tools` | `AgentBuilder::retrieved_tools(n, index, toolset)` / `retrieved_tools_handler`: a `TopNIds` retrieval at the request boundary advertising the named tools | crates/rig-agent/src/agent/builder.rs:675,692; crates/rig-ecs/CONTRACT.md §12 (Retrievable grants) | tests/providers/gemini/cassette/corpus_retrieval.rs (retrieved_tools cells) and tests/providers/gemini/cassette/dynamic_tools.rs (3 rows); goldens gemini_retrieval_retrieved_tools_* | `gemini` |
| `run-model-candidate` | `AgentRunner::using_model(label)` / `using_model_value(model)` and `TypedRun::using_model`: a run-scoped default model candidate that routing hooks may still replace | crates/rig-agent/src/agent/runner.rs:152,161; crates/rig-agent/README.md "Runtime model routing"; crates/rig-ecs/CONTRACT.md §9.2 (`UsesModel` on the run) | needs a new recording or a synthetic model: no root test calls `using_model` (0 files under tests/). Companion rows: rig-agent::runtime_model_swapping::* (agent-engine, mock). Closest cassettes: tests/cassettes/anthropic/corpus_serving/model_route* (a route selected by a hook, not a run candidate) | `anthropic` |
| `agent-set-model` | `Agent::set_model` / `with_model` / `set_model_ref` / `ModelHandle`: replacing the default model on one agent value (clones keep their own default; scoped registration leaves the bus when the last selector drops) | crates/rig-agent/src/agent/completion.rs:461-505; crates/rig-agent/README.md "Runtime model routing" | needs synthetic or a second recording: no root test calls `set_model`/`with_model` (corpus_shaping uses `register_model` only, tests/providers/anthropic/cassette/corpus_shaping.rs:274). Companion rows: rig-agent::runtime_model_swapping::* and rig-agent::rig_agent::bus::handle::tests::* (mock) | `anthropic` |
| `invalid-call-hook-resolution` | `AgentHook::on_invalid_tool_call` resolving an unadvertised call with `Retry { feedback }` / `Repair { tool_name }` / `Skip { reason }` / `Stop` | crates/rig-agent/src/agent/hook.rs:1332; AGENTS.md "Agent Hook Changes"; crates/rig-ecs/CONTRACT.md §8.2 (`Resolution`) | synthetic only, by construction: no recorded model emits a call to a tool it was not offered (crates/rig-verify/tests/corpus_invalid.rs doc). Producers: tests/core/golden_invalid.rs (13), tests/core/golden_recovery.rs (1), tests/core/golden_delta.rs (11) over MockCompletionModel, goldens mock_invalid_*/mock_delta_*/mock_invalid_tool_call_recovery (rig-verify rows verify-corpus-engine/-hand-driver/-world). tests/providers/anthropic/cassette/response_identity_edge.rs (14 unclassified rows) names the hook once | `runtime` |
| `invalid-call-runner-policy` | `AgentRunner::unhandled_invalid_tool_call(Ignore | Fail)` and `max_invalid_tool_call_retries(n)`: the run-level policy when no hook resolves an invalid call, and the retry budget | crates/rig-agent/src/agent/runner.rs:298,358; crates/rig-agent/src/run/spec.rs (`UnhandledInvalidToolCall`); crates/rig-ecs/CONTRACT.md §8.2 (`InvalidCalls.unhandled`, `InvalidRetries`) | tests/providers/gemini/cassette/agent_run_recovery.rs (5 rows, `max_invalid_tool_call_retries`) and agent_run_resume.rs (3 rows) with their gemini cassettes; the Ignore/Fail policy only in mock producers tests/core/golden_delta.rs, golden_invalid.rs, golden_outcome.rs | `gemini` |
| `reasoning-delta-hook` | `AgentHook::on_reasoning_delta`: observing (and stopping on) streamed reasoning deltas scoped by correlator | crates/rig-agent/src/agent/hook.rs:1356; crates/rig-ecs/README.md hook table (`on_reasoning_delta` → a system after `RigSet::Fold` on `Changed<Streamed>`) | tests/providers/anthropic/cassette/reasoning_roundtrip.rs::reasoning_delta_hook_streaming with tests/cassettes/anthropic/reasoning_delta_hook/streaming.yaml (also gemini, openai, openrouter reasoning_roundtrip.rs). tests/providers/anthropic/cassette/reasoning_delta_hook.rs exists but is registered in no binary (absent from the compiled listing) | `anthropic` |
| `run-suspend-resume` | `AgentRunner::resume(run)` on a serialized `AgentRun` (suspend between steps, resume in another process) and `rig_effect_log::Checkpoint` / `EffectLog::from_checkpoint` | crates/rig-agent/src/agent/runner.rs:116; crates/rig-agent/README.md "The run protocol"; crates/rig-effect-log/src/log.rs:158-220; crates/rig-ecs/CONTRACT.md §13 | tests/providers/gemini/cassette/agent_run_resume.rs (3 rows: `AgentRun` serialized mid tool execution and resumed) with its cassettes; tests/providers/ollama/pause_control.rs (1 row, `.resume(`); tests/core/agent_run_stepper.rs (2, uncatalogued); companion rows rig-verify::corpus_resume::resumed::*, corpus_checkpoint::{payload,hash,full_log_refused}::*, durable_execution::* (agent, goldens) | `gemini` |
| `run-tool-context` | `AgentRunner::tool_context(ToolContext)` / `TypedRun::tool_context`: a typed per-run context cloned into every tool dispatch (rig-ecs: `ToolContextSpec` → `bus::ToolInputs`) | crates/rig-agent/src/agent/runner.rs:172; crates/rig-agent/src/agent/typed.rs:111; crates/rig-agent/README.md ("Classic tools ... receive `&mut ToolContext`"); crates/rig-ecs/CONTRACT.md §8.4 | existing fixture: any tool-call cassette, the context is off the wire (CONTRACT §8.4): tests/cassettes/anthropic/effect_corpus (tool_call_turn). No root test calls `tool_context(` (0 files under tests/). Companion rows: rig-agent::rig_agent::agent::tool::tests::context_propagates_into_sub_agent, rig-agent::rig_agent::tool::registry::tests::* (mock) | `anthropic` |
| `run-documents` | `AgentRunner::document` / `documents`: static context documents appended for one run (beside the agent's `context` documents) | crates/rig-agent/src/agent/runner.rs:201,207; crates/rig-ecs/CONTRACT.md §1 (`documents`: the turn's `Attachment` links) | existing fixture: tests/cassettes/anthropic/corpus_request_shape (static_context cell; agent row rig::anthropic::anthropic::cassette::corpus_request_shape::static_context uses the builder's `.context`). No root test calls the runner's `.document(`/`.documents(` on an agent run (the 21 files matching are embedding/document-upload suites) | `anthropic` |
| `run-memory-override` | `AgentRunner::without_memory()` (no load, no append for one run) and `AgentRunner::conversation(id)` overriding the agent's conversation for one run | crates/rig-agent/src/agent/runner.rs:344,350; crates/rig-ecs/CONTRACT.md §11 | existing fixture: tests/cassettes/anthropic/corpus_memory (history_bypass, memory_conversation cells: agent rows). No root test calls `without_memory` (0 files under tests/); companion rows rig-agent::rig_agent::agent::streaming::tests::streaming_without_memory_disables_for_request (mock) | `anthropic` |
| `content-telemetry` | `AgentBuilder::record_content_telemetry(bool)` / `AgentRunner::record_content_telemetry`: message content recorded into OpenTelemetry span attributes (off by default) | crates/rig-agent/src/agent/builder.rs:276; crates/rig-agent/src/agent/runner.rs:312 | needs synthetic: a tracing subscriber capture; no root test sets it (0 files under tests/). Companion rows rig-agent::rig_agent::agent::streaming::tests::{unary_prompt_message_telemetry_records_accepted_output_when_opted_in, tool_arguments_and_results_follow_content_telemetry_toggle} (mock). CONTRACT §1 notes `record_telemetry_content` never appears on the wire and names no ECS equivalent | `runtime` |
| `apply-spec` | `AgentBuilder::apply_spec(RunSpec)` / `Agent::run_spec()`: configuring an agent from a serializable run spec and reading it back | crates/rig-agent/src/agent/builder.rs:297; crates/rig-agent/src/agent/completion.rs:397; crates/rig-agent/README.md "The run protocol" (`RunSpec`) | needs synthetic: no root test calls `apply_spec` (0 files under tests/). Companion rows rig-agent::rig_agent::run::spec::tests::* (agent-run-protocol). rig-ecs has `replay::spec_json(world, run)` (CONTRACT §6, §10) | `runtime` |
| `dynamic-tools` | `AgentBuilder::dynamic_tool` / `portable_dynamic_tool` / `dynamic_tools` (runtime-defined tools), `rig_core::tool::PortableTool` and `#[rig_tool]` functions | crates/rig-agent/src/agent/builder.rs:657-670; crates/rig-agent/README.md ("Portable tools implement `rig_core::tool::PortableTool` and work in both runtimes"); crates/rig-agent/src/lib.rs (`rig_tool` re-export) | tests/providers/gemini/cassette/dynamic_tools.rs (3 rows) and tests/providers/gemini/cassette/tool_definitions.rs (3 rows, `#[rig_tool]`) with their gemini cassettes; tests/tool_facade_traits.rs and tests/fixtures/tool_facade (uncatalogued, compile-level); companion rows rig-agent::rig_agent::tool::migrated_tests::* (portable fixtures) | `gemini` |
| `builtin-tools` | `rig_agent::tool::builtin` (the built-in portable tools, e.g. the think tool) on an agent | crates/rig-agent/src/tool/builtin.rs (re-export of `rig_core::tool::builtin`) | tests/providers/anthropic/cassette/think_tool.rs (1 row) and think_tool_with_other_tools.rs (1 row) with tests/cassettes/anthropic/think_tool* | `anthropic` |
| `replay-identity-api` | `Agent::check_replayable(log)`, `required_row()`, `effect_log()`/`take_effect_log()`/`stamp(log)`, `into_parts()` and `tool_definitions()`: the agent's own replay-compatibility and introspection API | crates/rig-agent/src/agent/completion.rs:515-720 | tests/consumer/identity.rs and persistence.rs call `check_replayable` (ecs_consumer binary, uncatalogued rows; capabilities-owned); tests/providers/gemini/cassette/dynamic_tools.rs calls `tool_definitions` (unclassified). Companion rows rig-verify::log_header::*, golden_refusal::*, corpus_header::the_agent_names_* (agent, goldens) | `capabilities` |
| `streaming-feed-api` | `AgentRunner::run_channel()` / `RunEvents` (a bounded feed of `MultiTurnStreamItem`s beside the run) and `stream_to_stdout` | crates/rig-agent/src/agent/streaming.rs:354-472 | `stream_to_stdout` in tests/providers/{openai,openrouter,copilot,groq,deepseek,xai,chatgpt,mistral}/permission_control.rs (2 rows each, unclassified); no root test calls `run_channel` (0 files under tests/) | `openai` |
| `cli-chatbot` | `rig_agent::integrations::cli_chatbot` (`ChatBot`, `Chat` trait): the interactive stdin/stdout loop over an agent | crates/rig-agent/src/integrations/cli_chatbot.rs; crates/rig-agent/README.md ("runtime integrations") | needs synthetic: no test anywhere in tests/ or the companion crates (0 files) | `capabilities` |
| `memory-policies-on-agent` | rig-memory `SlidingWindowMemory` / `TokenWindowMemory` / `PolicyMemory` shaping the history an agent loads (the `agent_with_memory_policies` example) | crates/rig-memory/README.md; crates/rig-memory/examples/agent_with_memory_policies.rs; crates/rig-ecs/CONTRACT.md §11 (a memory handler is where the graph persists) | needs a new recording or synthetic: a trimming policy changes the request bytes, so tests/cassettes/anthropic/corpus_memory cannot be reused as is. Companion rows rig-memory::rig_memory::tests::* (66, infrastructure: policies on vectors, no agent) | `anthropic` |
| `wasm-agent-run` | an agent run on `wasm32-unknown-unknown` (rig-agent README "Target support": tier 2, full support with no feature flags) | crates/rig-agent/README.md "Target support"; crates/rig-agent/src/lib.rs crate docs | rig-ecs has executed evidence (crates/rig-ecs/tests/run_wasm.rs: two goldens through the graph on wasm, companion rows rig-ecs::run_wasm::*); rig-agent has only the bus on wasm (crates/rig-agent/tests/bus_wasm.rs, 2 rows): no rig-agent test runs an agent on the browser target | `capabilities` |
| `mcp-tools` | MCP tools through the companion `rig-rmcp` crate (native-only) | crates/rig-agent/README.md "Target support" and crates/rig-agent/src/lib.rs (MCP support lives in rig-rmcp) | tests/providers/gemini/cassette/tool_server.rs (3 rows) and dynamic_tools.rs mention rmcp; crates/rig-rmcp is outside the five inventoried companion crates | `gemini` |
| `hook-self-naming` | `AgentHook::name()`: a hook naming itself (with its state) in the log header's hook list, so two programs differing only in hook state are distinct | crates/rig-agent/src/agent/hook.rs (`AgentHook::name`); crates/rig-ecs/CONTRACT.md §10 (`LogHeader::hooks`) | tests/providers/anthropic/cassette/corpus_oracle.rs (stop_after_turn_two cell, `StopAfterTurnN` in tests/common/goldens.rs:1335) with tests/cassettes/anthropic/corpus_oracle; golden anthropic_oracle_stop_after_turn_two (rig-verify rows) | `anthropic` |
| `output-tool-name` | `AgentRun::with_output_tool_name` / `set_output_tool_name` and the runner-side output-tool naming (rig-ecs: `OutputToolConfig`) | crates/rig-agent/src/run/mod.rs:762-782; crates/rig-ecs/CONTRACT.md §2 (`OutputToolConfig`) | the default `final_result` naming and the extractor's `submit` name are covered by the corpus_output and extractor_usage agent rows; the explicit rename API is exercised only by tests/fixtures/agent_run_stepper (uncatalogued fixture crate) and, natively, crates/rig-ecs/tests/run_output_tool_config.rs (9 rows, ecs-agent-graph) | `runtime` |

23 gaps: anthropic 9, gemini 6, openai 1, runtime 4, capabilities 3.

## Exercised by root `agent` rows (not gaps)

| capability | evidence |
|---|---|
| builder: preamble/without_preamble/append_preamble/context/tool_choice/default_max_turns/temperature/max_tokens/additional_params/output_schema/output_mode/memory/memory_handler/conversation/model_route/owner/configure_bus/with_bus_config/over_bus/tool_server_handle/tool/record_effects{,_with_events}/add_hook | anthropic corpus_* agent rows (tests/providers/anthropic/cassette/corpus_{request_shape,output,memory,serving,shaping,host,layers,causal}.rs) and provider agent.rs rows |
| runner: max_turns/history/preamble/temperature/max_tokens/additional_params/tool_choice/tool_concurrency/conversation | the same rows plus tests/providers/*/agent_tool_sessions.rs and streaming_tools.rs |
| Agent::prompt/stream_prompt/chat/stream_chat/prompt_typed/register_model | agent rows across provider trees (chat: deepseek/xai/openrouter agent_tool_sessions; prompt_typed: openrouter agent_tool_sessions; register_model: anthropic corpus_shaping late_route) |
| Extractor (build, extract, retries, dynamic_context on an extractor) | extractor_usage.rs agent rows (copilot, deepseek, llamacpp, openai, openrouter, xai) and turn_termination_matrix.rs |
| hooks: on_run_start (observe, rewrite, dispatch, stop), on_run_settled, on_model_select, on_completion_call (patch, dispatch, stop), on_model_turn_finished, on_text_delta, on_tool_call_delta, on_dispatch, on_outcome, HookStack nesting, Scratchpad, HookContext::bind (RunHandle) | lifecycle_matrix.rs (LifecycleHookProbe in tests/common/support.rs), anthropic corpus_{hooks,endings,host,serving,shaping}.rs with tests/common/goldens.rs hooks, gemini hook_stress*.rs |
| AgentRun::next_step hand driver on a cassette | tests/providers/gemini/cassette/agent_run_streamed.rs (6 agent rows) |

## Not capabilities (excluded)

`test_utils::model_conformance` validators (test helpers), `AgentRun` value serde (companion rows agent-value-types), the `prelude`/`core` namespace re-exports (compile tests under tests/tool_facade_*.rs, uncatalogued root registrations reported separately).
