[package]
name = "rig-contextual-bevy-rejection-fixture"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
rig = { path = "../../../..", default-features = false, features = ["agent", "bevy"] }
serde = { version = "1.0.219", features = ["derive"] }
serde_json = "1.0.150"
