when:
  path: /chat/completions
  method: POST
  query_param: []
  header:
  - name: accept
    value: '*/*'
  - name: content-type
    value: application/json
  body: '{"max_tokens":8,"messages":[{"content":"Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Answer with exactly one word: cached.","role":"user"}],"model":"deepseek-v4-flash","thinking":{"type":"disabled"}}'
then:
  status: 200
  header:
  - name: content-type
    value: application/json
  body: '{"choices":[{"finish_reason":"stop","index":0,"logprobs":null,"message":{"content":"cached","role":"assistant"}}],"created":0,"id":"chatcmpl-REDACTED_1","model":"deepseek-v4-flash","object":"chat.completion","system_fingerprint":"a26a7955944dc5c60445bff77fac9c8e","usage":{"completion_tokens":2,"prompt_cache_hit_tokens":0,"prompt_cache_miss_tokens":900,"prompt_tokens":900,"prompt_tokens_details":{"cached_tokens":0},"total_tokens":902}}'
---
when:
  path: /chat/completions
  method: POST
  query_param: []
  header:
  - name: accept
    value: '*/*'
  - name: content-type
    value: application/json
  body: '{"max_tokens":8,"messages":[{"content":"Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Cache probe paragraph. The nightly build pipeline warms the artifact cache, uploads the artifacts, verifies the checksums, publishes the manifest, and finally notifies the release channel. Answer with exactly one word: cached.","role":"user"}],"model":"deepseek-v4-flash","thinking":{"type":"disabled"}}'
then:
  status: 200
  header:
  - name: content-type
    value: application/json
  body: '{"choices":[{"finish_reason":"stop","index":0,"logprobs":null,"message":{"content":"cached","role":"assistant"}}],"created":0,"id":"chatcmpl-REDACTED_2","model":"deepseek-v4-flash","object":"chat.completion","system_fingerprint":"a26a7955944dc5c60445bff77fac9c8e","usage":{"completion_tokens":2,"prompt_cache_hit_tokens":896,"prompt_cache_miss_tokens":4,"prompt_tokens":900,"prompt_tokens_details":{"cached_tokens":896},"total_tokens":902}}'
