mod streaming_conformance;
mod support;

mod cassette {
    mod agent;
    mod document_ordering;
    mod embeddings;
    mod model_ids;
    mod raw_completion;
    mod raw_provider_data;
    mod raw_streaming;
    mod streaming;
    mod tool_choice;
}
